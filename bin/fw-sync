#!/bin/bash
# Convenience wrapper: run full mirror sync and push updates.
set -euo pipefail
SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
REPO_ROOT=$(cd "$SCRIPT_DIR/.." && pwd)
exec "$REPO_ROOT/bin/file_window_sync" --push "$@"
