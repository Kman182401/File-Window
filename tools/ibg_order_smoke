#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
PROJ_DIR="$(cd -- "${SCRIPT_DIR}/.." && pwd)"
VENV_BIN="${PROJ_DIR}/.venv/bin"
exec "${VENV_BIN}/python" "${PROJ_DIR}/tools/ibg_order_smoke.py" "$@"

