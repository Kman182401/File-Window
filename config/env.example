# Local runtime configuration for M5-Trader
# Copy these into ~/.config/m5_trader/env.local and adjust as needed.

# IBKR connection (Paper)
IBKR_HOST=127.0.0.1
IBKR_PORT=4002
IBKR_CLIENT_ID=9002

# Order controls
ALLOW_ORDERS=1
DRY_RUN=0
PAPER_ENFORCE=1
MAX_QTY_FUT=1
MAX_DAILY_LOSS_PCT=1.0

# Paths
DATA_DIR=$HOME/.local/share/m5_trader/data
CACHE_DIR=$HOME/.cache/m5_trader
MODELS_DIR=$HOME/models
LOG_DIR=$HOME/logs

# Cloud/S3 (0 disables)
S3_ENABLE=0

# Notes:
# - Keep Read-Only API OFF in IB Gateway (Paper) for paper orders.
# - Keep Trusted IPs to 127.0.0.1 and Socket port 4002.
# - Do NOT commit your ~/.config/m5_trader/env.local to git.

